# Docker Environment Configuration for COPRRA
# This file is specifically for Docker containerized environments
# DO NOT commit this file with sensitive values to version control

# Application
APP_NAME="COPRRA"
APP_ENV=production
APP_KEY=base64:Vq/oWPvNqNXxIModKBOhgrXT1LY8MCCddzldehHx70M=
APP_DEBUG=false
APP_URL=http://localhost

# Database (Docker container names)
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=YOUR_DB_PASSWORD
DB_USERNAME=YOUR_DB_PASSWORD
DB_PASSWORD=YOUR_DB_PASSWORD
DB_ROOT_PASSWORD=YOUR_ROOT_PASSWORD

# Cache & Session
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# Redis (Docker container name)
REDIS_HOST=redis
REDIS_PASSWORD=
REDIS_PORT=6379
REDIS_CLIENT=phpredis

# Mail (Mailpit in Docker)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@coprra.local"
MAIL_FROM_NAME="${APP_NAME}"

# Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Docker Ports
NGINX_PORT=80
NGINX_SSL_PORT=443
DB_PORT=33061
REDIS_PORT=6379
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# Performance
OPCACHE_ENABLE=1

# Security
REQUIRE_2FA=true
PASSWORD_MIN_LENGTH=12
SESSION_LIFETIME=120

# COPRRA Specific
COPRRA_DEFAULT_CURRENCY=USD
COPRRA_DEFAULT_LANGUAGE=en
PRICE_CACHE_DURATION=3600
MAX_STORES_PER_PRODUCT=10
API_RATE_LIMIT=100
